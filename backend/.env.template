# ============================================
# Isla Cloud Solutions - Backend API
# Copia este archivo como .env y edita los valores
# cp .env.template .env
# ============================================

# Puerto del servidor API
PORT=3010

# Base de datos MariaDB
DB_HOST=localhost
DB_PORT=3306
DB_USER=ics_admin_new
DB_PASSWORD=KznD85wyt7tNov
DB_NAME=ics_new

# JWT (genera una clave segura: openssl rand -hex 64)
JWT_SECRET=GENERA_UNA_CLAVE_SECRETA_LARGA_Y_SEGURA
JWT_EXPIRES_IN=24h

# CORS - Dominios permitidos (separados por coma para múltiples orígenes)
# Las previews de Lovable (.lovable.app y .lovableproject.com) ya quedan permitidas automáticamente por el backend.
CORS_ORIGIN=https://www.islacloudsolutions.com,https://islacloudsolutions.com

# Directorio de uploads (relativo al backend)
UPLOAD_DIR=./uploads

# SMTP - Notificaciones por email (Nodemailer)
# Configura tu servidor SMTP para recibir emails de contacto
SMTP_HOST=mail.islacloudsolutions.com
SMTP_PORT=587
SMTP_USER=info@islacloudsolutions.com
SMTP_PASS=TU_PASSWORD_DE_EMAIL
SMTP_FROM_NAME=Isla Cloud Solutions

# OpenAI - Traducción automática de contenidos CMS
OPENAI_API_KEY=sk-TU_API_KEY_DE_OPENAI
